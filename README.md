# bioeditor-play

Static GitHub Pages deploy of [BioEditor](https://github.com/steveh-alpha/BioEditor).

**SOURCE_SHA:** `6ff8dde79ff49b84f2145f2c898e201dee43ff4b` (homepage + DNA pack)

- Apex `index.html` is the Codex homepage (H1 "Good science" / "Beautifully explained").
- Editor: `editor.html`
- `app.js` is a tiny gzip loader; exact source is `app.js.gz.b64` (sha256 of decompressed = sha256 of BioEditor `src/app.js` at SOURCE_SHA).

Do not treat `marketing/` as the site apex.
